import model.EnemySkillDataJson
import model.CardDataJson
import scala.io.Source

object MySuite {
  // seems like metals or vscode does not recognize scala 3's @main, so we need to define main like this
  def main(args: Array[String]): Unit = {
    println("asdfasdfasdfasdf")
  }
}
